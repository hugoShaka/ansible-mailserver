---
# Requires Ansible >= 1.9
- hosts: localhost
  tasks:
    - name: Install acl
      apt:
        name: acl
        state: installed
        update_cache: true
  become: true
  become_user: root

- hosts: localhost
  roles:
    - ispmail-certificate
    - ispmail-database
    - ispmail-postfix
    - ispmail-dovecot
  become: true
  become_user: root
